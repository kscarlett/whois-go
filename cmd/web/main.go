package main

func main() {

}

// embed static page with search box for root page
// search box goes to {base}/{search_term}
// anything that isn't root searches for {search_term}, return error if error, return result if no error
// rate limit each client
